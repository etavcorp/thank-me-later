{
  "name": "thank-me-later-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "npm@10.9.2",
  "volta": {
    "node": "22.19.0",
    "npm": "10.9.2"
  },
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy --env prd",
    "deploy:uat": "wrangler deploy --env uat",
    "deploy:prd": "npm --prefix ../frontend install && npm --prefix ../frontend run build && wrangler deploy --env prd",
    "publish": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
